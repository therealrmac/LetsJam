@model IEnumerable<LetsJam.Models.Relation>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ConnectedOn)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Connected)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConnectedOn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Connected)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RelationshipId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RelationshipId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RelationshipId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
